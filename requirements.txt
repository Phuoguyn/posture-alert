# Streamlit Cloud-safe set (Python 3.11)
streamlit>=1.51,<1.52
streamlit-webrtc==0.63.11

# WebRTC stack
aiortc==1.14.0
av==14.0.1
pylibsrtp
pyee
pyopenssl

# Vision + posture
mediapipe==0.10.14
opencv-python-headless==4.9.0.80
numpy==1.26.4
pandas==2.3.3

# MediaPipe needs protobuf 4.x (NOT <4)
protobuf>=4.25.3,<5
# Streamlit 1.5x expects pyarrow 21.x–22.x; MediaPipe doesn’t need it,
# so omit unless you use st.dataframe with Arrow.
pyarrow==21.0.0
